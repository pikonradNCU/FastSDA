# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

Plugins, "SDA2D3D", pl.umk.mat.fastSDA.imageJ.sdaPlugin.Process_SDA
#Plugins, "SoftSDA", net.pikonrad.imageJ.softSdaPlugin.SoftSda
#Process, "Process DataCollection", net.pikonrad.imageJ.Process_pikoDataCollection
#Process, "Process Piko Test", net.pikonrad.imageJ.sdaPlugin.PikoTest
